# plan8 — Modern CSS scroll-driven progress bar

## Prompt to AI
Add a reading progress bar using animation-timeline: scroll() with @supports fallback.

## Plan
- Add fixed top bar
- Use @supports(animation-timeline: scroll()) for modern browsers
- Fallback to JS later if needed (optional)

## Implemented
- Added scroll.css + progress element
