# plan18 — Open Graph image placeholder

## Prompt to AI
Create a lightweight OG image placeholder so social metadata does not reference a missing file.

## Plan
- Add a simple SVG OG image using brand colors
- Match typical OG size (1200x630)
- Keep text generic so it can be replaced later

## Implemented
- Added og-image.svg in assets/images
- Metadata already points to this path
