# plan4 — Base typography + reusable components

## Prompt to AI
Create CSS for fonts, headings, section padding, neon links, card styling, and a glass navbar.

## Plan
- Apply font stacks to body/h1/h2
- Make section spacing reusable
- Create neon link style + card accents
- Keep Bootstrap defaults intact; override lightly

## Implemented
- Updated base.css + style.css with tokens + components
