# plan13 — .gitignore

## Prompt to AI
Create a .gitignore for a static web repo that uses Tailwind CLI (node) and standard OS/editor artifacts.

## Plan
- Ignore node_modules and npm logs
- Ignore OS/editor junk
- Keep built CSS tracked (tailwind.output.css) because this is a static deploy

## Implemented
- Added .gitignore at repo root
